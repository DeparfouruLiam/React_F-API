version: "3.9"

services:
  backend:
    build: .
    container_name: backend
    ports:
      - "8000:8000"

  frontend:
    build: ./my-react-app
    container_name: frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
