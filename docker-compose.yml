services:
  backend:
    build: .
    container_name: backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./my-react-app
      args:
        VITE_API_URL: http://backend:8000
    container_name: frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
